version: "3.8"
services:
  spring:
    build: .
    ports:
      - "8080:8080"
    depends_on:
      - redis
    extra_hosts:
      - "host.docker.internal:host-gateway"

  redis:
    image: redis
    ports:
      - "6379:6379"
